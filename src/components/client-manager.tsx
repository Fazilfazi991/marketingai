"use client";
import Link from "next/link";
import {useState} from "react";
import {clients as seed} from "@/lib/demo-data";
import {Plus,SlidersHorizontal,X} from "lucide-react";
import {Panel,Status} from "./ui";
type Client={name:string;services:string;status:string;health:string;progress:number;access:string;owner:string};
const seeded:Client[]=seed.map(client=>({name:client.name,services:client.services,status:client.status,health:client.health,progress:client.progress,access:client.access,owner:client.owner}));
export function ClientManager(){const [items,setItems]=useState(seeded),[open,setOpen]=useState(false),[notice,setNotice]=useState("");function submit(data:FormData){const name=String(data.get("name")||"").trim();if(!name)return;const services=["seo","social","blogs","maintenance","chatbot","whatsapp","reporting"].filter(key=>data.get(key)).length;const item:Client={name,services:`${services} active services`,status:"Onboarding",health:"Healthy",progress:10,access:"Access checklist pending",owner:String(data.get("owner"))};setItems(current=>[item,...current]);setOpen(false);setNotice(`${name} created with an internal service scope.`)}return <>{notice&&<div className="toast">{notice}</div>}<Panel title="All clients" meta={`${items.filter(x=>x.status==="Active").length} active · ${items.filter(x=>x.status==="Onboarding").length} onboarding`} action={<button className="button" onClick={()=>setOpen(true)}><Plus size={15}/>New client</button>}><div className="toolbar"><button className="filter">All clients</button><button className="filter">Active</button><button className="filter">Onboarding</button><button className="filter"><SlidersHorizontal size={12}/>Filters</button></div><div className="table-wrap"><table className="data-table"><thead><tr><th>Client</th><th>Service scope</th><th>Health</th><th>Onboarding</th><th>Access</th><th>Owner</th></tr></thead><tbody>{items.map((client,index)=><tr key={`${client.name}-${index}`}><td><div className="client-name"><span className="avatar">{client.name.split(" ").map(part=>part[0]).join("").slice(0,2)}</span><span><Link className="client-link" href={client.name==="ABC Interiors"?"/admin/clients/abc-interiors":"/admin/clients"}>{client.name}</Link><small>{client.status}</small></span></div></td><td><b>{client.services}</b></td><td><Status tone={client.health==="Needs attention"?"warn":client.health==="At risk"?"risk":""}>{client.health}</Status></td><td><div className="mini-progress"><span><i style={{width:`${client.progress}%`}}/></span>{client.progress}%</div></td><td>{client.access}</td><td>{client.owner}</td></tr>)}</tbody></table></div></Panel>{open&&<div className="modal-wrap" role="dialog" aria-modal="true"><form className="modal" action={submit}><header><h2>Create client</h2><button type="button" className="ghost-icon" onClick={()=>setOpen(false)} aria-label="Close"><X size={18}/></button></header><div className="modal-body form-grid"><div className="field full"><label>Business name</label><input name="name" required placeholder="Business name"/></div><fieldset className="field full"><legend>Active services</legend><div className="service-scope-options">{[["seo","SEO"],["social","Social Media · 12 posts/month"],["blogs","Blogs · 2/month"],["maintenance","Website Maintenance"],["chatbot","Website Chatbot"],["whatsapp","WhatsApp AI"],["reporting","Analytics / Reporting"]].map(([key,label])=><label key={key}><input type="checkbox" name={key} defaultChecked/> {label}</label>)}</div></fieldset><div className="field"><label>Assigned staff</label><select name="owner" defaultValue="Maya"><option>Maya</option><option>Omar</option><option>Unassigned</option></select></div><div className="field"><label>Industry</label><input name="industry" placeholder="e.g. Real estate"/></div><div className="field"><label>Location</label><input name="location" placeholder="Dubai, UAE"/></div></div><div className="modal-actions"><button type="button" className="button secondary" onClick={()=>setOpen(false)}>Cancel</button><button className="button">Create client</button></div></form></div>}</>}
