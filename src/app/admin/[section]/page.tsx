import { AppShell } from "@/components/app-shell"; import { Panel, Status } from "@/components/ui";
const copy:Record<string,[string,string]>={approvals:["Approval queue","14 items are ready for partner review."],seo:["SEO workspace","Keywords, pages and opportunities across every client."],reports:["Reports","Monthly results and next-focus summaries."],assets:["Asset library","Brand, original and generated files by client."],automations:["Automations","Workflow health, runs and visible failures."]};
export default async function Section({params}:{params:Promise<{section:string}>}){const {section}=await params;const [title,subtitle]=copy[section]??["Workspace","This module is being prepared."];return <AppShell role="admin" title={title} subtitle={subtitle}><Panel title="Foundation ready" meta="Connected to the Gro operating model"><div className="task-item"><div><b>{title} data model is prepared</b><p>Live integration follows after the core local workflow is verified.</p></div><Status tone="purple">Demo</Status></div></Panel></AppShell>}
