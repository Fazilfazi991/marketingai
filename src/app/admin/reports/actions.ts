"use server";import {revalidatePath} from "next/cache";import {createClient} from "@/lib/supabase/server";
type Result={ok:true}|{ok:false;error:string};
async function adminContext(clientId:string){const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)throw new Error("Sign in as a partner.");const{data:membership}=await supabase.from("organization_members").select("organization_id").eq("user_id",user.id).eq("role","admin").eq("status","active").limit(1).maybeSingle();if(!membership)throw new Error("An active partner membership is required.");const{data:client}=await supabase.from("clients").select("id,name").eq("id",clientId).eq("organization_id",membership.organization_id).is("deleted_at",null).maybeSingle();if(!client)throw new Error("Client not found.");return{supabase,client}}
export async function publishReport(id:string,clientId:string):Promise<Result>{if(!/^[0-9a-f-]{36}$/i.test(id))return{ok:false,error:"Invalid report."};try{const{supabase}=await adminContext(clientId);const{error}=await supabase.from("reports").update({status:"published"}).eq("id",id).eq("client_id",clientId);if(error)return{ok:false,error:error.message};revalidatePath("/admin/reports");revalidatePath("/client");revalidatePath("/client/reports");return{ok:true}}catch(error){return{ok:false,error:error instanceof Error?error.message:"Unable to publish the report."}}}
export async function regenerateReport(clientId:string,month:string):Promise<Result>{if(!/^\d{4}-\d{2}$/.test(month))return{ok:false,error:"Choose a valid report month."};try{const{supabase,client}=await adminContext(clientId),start=`${month}-01`,endDate=new Date(`${start}T00:00:00Z`);endDate.setUTCMonth(endDate.getUTCMonth()+1);const end=endDate.toISOString().slice(0,10);const[{data:analytics},{data:search},{data:content},{data:period}]=await Promise.all([supabase.from("analytics_daily").select("metrics").eq("client_id",clientId).gte("day",start).lt("day",end),supabase.from("search_console_daily").select("metrics").eq("client_id",clientId).gte("day",start).lt("day",end),supabase.from("content_items").select("content_kind,status").eq("client_id",clientId).eq("month",start),supabase.from("delivery_periods").select("id,delivery_obligations(label,delivered_quantity)").eq("client_id",clientId).eq("month",start).maybeSingle()]);const metric=(rows:Array<{metrics:unknown}>|null,key:string)=>(rows??[]).reduce((sum,row)=>sum+(Number((row.metrics as Record<string,unknown>)?.[key])||0),0),users=metric(analytics,"users")||metric(analytics,"visitors"),clicks=metric(search,"clicks"),posts=(content??[]).filter(item=>item.content_kind==="social_post"&&item.status==="published").length,obligations=((period?.delivery_obligations??[]) as Array<{label:string;delivered_quantity:number}>),work=obligations.filter(item=>item.delivered_quantity>0).map(item=>`${item.delivered_quantity} ${item.label.toLowerCase()} delivered`);const summary=`${client.name} recorded ${users.toLocaleString()} website visitors and ${clicks.toLocaleString()} organic search clicks this month. ${posts} social posts were published.`;const{error}=await supabase.from("reports").upsert({client_id:clientId,month:start,summary,work_completed:work,analytics_summary:{users,user_change:0,clicks,click_change:0,social_posts:posts},next_month_focus:"Review the strongest lead and search sources, then prioritize the work most likely to improve qualified enquiries.",status:"needs_review"},{onConflict:"client_id,month"});if(error)return{ok:false,error:error.message};revalidatePath("/admin/reports");return{ok:true}}catch(error){return{ok:false,error:error instanceof Error?error.message:"Unable to regenerate the report."}}}
