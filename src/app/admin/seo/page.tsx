import {AppShell} from "@/components/app-shell";import {AdminSeo} from "@/components/admin-growth-workspaces";import {loadAdminGrowth} from "@/lib/admin-growth-data";
export default async function SeoPage(){const data=await loadAdminGrowth();return <AppShell role="admin" title="SEO workspace" subtitle="Keywords, target pages and practical opportunities across every client."><AdminSeo keywords={data.keywords} opportunities={data.opportunities} clients={data.clients} isDemo={data.isDemo}/></AppShell>}
