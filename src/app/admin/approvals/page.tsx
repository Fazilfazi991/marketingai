import {AppShell} from "@/components/app-shell";import {ApprovalQueue} from "@/components/approval-queue";import {Panel} from "@/components/ui";import {loadAdminApprovals} from "@/lib/admin-approval-data";
export default async function Approvals(){const data=await loadAdminApprovals();return <AppShell role="admin" title="Approval queue" subtitle="Review social, blog and SEO work without opening every client."><Panel title="Needs review" meta="Human approval is required before publishing"><ApprovalQueue initial={data.items} isDemo={data.isDemo}/></Panel></AppShell>}
