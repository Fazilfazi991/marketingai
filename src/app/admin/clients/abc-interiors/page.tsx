import { AppShell } from "@/components/app-shell"; import { ClientWorkspace } from "@/components/client-workspace";
export default function ABC(){return <AppShell role="admin" title="Client workspace" subtitle="What we promised, what is delivered, and what needs attention."><ClientWorkspace section="overview"/></AppShell>}
