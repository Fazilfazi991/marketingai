import { AppShell } from "@/components/app-shell"; import { Panel, Status } from "@/components/ui"; import { clients } from "@/lib/demo-data"; import { Plus, SlidersHorizontal } from "lucide-react";
export default function Clients(){return <AppShell role="admin" title="Clients" subtitle="Manage plans, access and delivery health." actions={<button className="button"><Plus size={15}/>New client</button>}><Panel title="All clients" meta="10 active · 2 onboarding"><div className="toolbar"><button className="filter">All clients</button><button className="filter">Active</button><button className="filter">Onboarding</button><button className="filter"><SlidersHorizontal size={12}/> Filters</button></div><div className="table-wrap"><table className="data-table"><thead><tr><th>Client</th><th>Plan</th><th>Health</th><th>Onboarding</th><th>Access</th><th>Renewal</th><th>Owner</th></tr></thead><tbody>{clients.map((c,i)=><tr key={c.name}><td><div className="client-name"><span className="avatar">{c.name.split(" ").map(x=>x[0]).join("")}</span><span><b>{c.name}</b><small>{c.status}</small></span></div></td><td><b>{c.plan}</b><br/><small>{c.value}/mo</small></td><td><Status tone={i===0?"warn":i===2?"risk":""}>{c.health}</Status></td><td><div className="mini-progress"><span><i style={{width:`${c.progress}%`}}/></span>{c.progress}%</div></td><td>{c.access}</td><td>{c.renewal}</td><td>{c.owner}</td></tr>)}</tbody></table></div></Panel></AppShell>}
