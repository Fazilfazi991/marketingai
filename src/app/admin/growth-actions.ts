"use server";import {revalidatePath} from "next/cache";import {createClient} from "@/lib/supabase/server";import type {AdminBlog} from "@/lib/admin-growth-data";
type Result={ok:true;id?:string}|{ok:false;error:string};const blogStatuses=new Set(["research","topic_selected","brief","draft","internal_review","ready_to_publish","published"]),opportunityStatuses=new Set(["open","in_progress","awaiting_review","approved","complete"]),priorities=new Set(["low","medium","high"]);
async function adminContext(clientId:string){const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)throw new Error("Sign in as a partner.");const{data:membership}=await supabase.from("organization_members").select("organization_id").eq("user_id",user.id).eq("role","admin").eq("status","active").limit(1).maybeSingle();if(!membership)throw new Error("An active partner membership is required.");const{data:client}=await supabase.from("clients").select("id").eq("id",clientId).eq("organization_id",membership.organization_id).is("deleted_at",null).maybeSingle();if(!client)throw new Error("Client not found.");return{supabase,clientId:String(client.id)}}
export async function createBlog(formData:FormData):Promise<Result>{try{const clientId=String(formData.get("clientId")??""),title=String(formData.get("title")??"").trim(),keyword=String(formData.get("keyword")??"").trim();if(!title||!keyword)return{ok:false,error:"Title and target keyword are required."};const{supabase}=await adminContext(clientId);const{data,error}=await supabase.from("content_items").insert({client_id:clientId,content_kind:"blog",topic:title,target_keyword:keyword,concept:String(formData.get("brief")??"").trim(),seo_metadata:{intent:String(formData.get("intent")??"Commercial")},status:"research"}).select("id").single();if(error)return{ok:false,error:error.message};revalidatePath("/admin/blogs");return{ok:true,id:String(data.id)}}catch(error){return{ok:false,error:error instanceof Error?error.message:"Unable to create the article."}}}
export async function saveBlog(item:AdminBlog):Promise<Result>{const status=item.status.toLowerCase().replaceAll(" ","_");if(!blogStatuses.has(status))return{ok:false,error:"Invalid blog status."};try{const{supabase}=await adminContext(item.clientId);const{error}=await supabase.from("content_items").update({topic:item.title,target_keyword:item.keyword,concept:item.brief,body:item.excerpt,seo_metadata:{intent:item.intent,title:item.metaTitle,description:item.metaDescription},status,updated_at:new Date().toISOString()}).eq("id",item.id).eq("client_id",item.clientId).eq("content_kind","blog");if(error)return{ok:false,error:error.message};revalidatePath("/admin/blogs");return{ok:true}}catch(error){return{ok:false,error:error instanceof Error?error.message:"Unable to save the article."}}}
export async function createKeyword(formData:FormData):Promise<Result>{try{const clientId=String(formData.get("clientId")??""),priority=String(formData.get("priority")??"medium").toLowerCase();if(!priorities.has(priority))return{ok:false,error:"Invalid priority."};const{supabase}=await adminContext(clientId);const{data,error}=await supabase.from("seo_keywords").insert({client_id:clientId,keyword:String(formData.get("keyword")??"").trim(),intent:String(formData.get("intent")??""),target_url:String(formData.get("url")??"").trim(),current_position:Number(formData.get("current"))||null,previous_position:Number(formData.get("current"))||null,priority,status:"tracking",notes:String(formData.get("notes")??"").trim()||null}).select("id").single();if(error)return{ok:false,error:error.message};revalidatePath("/admin/seo");return{ok:true,id:String(data.id)}}catch(error){return{ok:false,error:error instanceof Error?error.message:"Unable to add the keyword."}}}
export async function updateOpportunity(id:string,clientId:string,status:string):Promise<Result>{const normalized=status.toLowerCase().replaceAll(" ","_");if(!opportunityStatuses.has(normalized))return{ok:false,error:"Invalid SEO status."};try{const{supabase}=await adminContext(clientId);const{error}=await supabase.from("seo_tasks").update({status:normalized,updated_at:new Date().toISOString()}).eq("id",id).eq("client_id",clientId);if(error)return{ok:false,error:error.message};revalidatePath("/admin/seo");return{ok:true}}catch(error){return{ok:false,error:error instanceof Error?error.message:"Unable to update the opportunity."}}}
