"use server";import {revalidatePath} from "next/cache";import {createClient} from "@/lib/supabase/server";import type {AdminBlog} from "@/lib/admin-growth-data";import type {GenerationContext} from "@/lib/ai/provider";import {createAIProvider} from "@/lib/ai/provider";import {DemoAIProvider} from "@/lib/ai/demo-provider";import {isDemoMode} from "@/lib/demo-mode";
type Result={ok:true;id?:string}|{ok:false;error:string};type GeneratedBlogResult={ok:true;article:Pick<AdminBlog,"brief"|"excerpt"|"metaTitle"|"metaDescription"|"status">;provider:string;model:string}|{ok:false;error:string};const blogStatuses=new Set(["research","topic_selected","brief","draft","internal_review","ready_to_publish","published"]),opportunityStatuses=new Set(["open","in_progress","awaiting_review","approved","complete"]),priorities=new Set(["low","medium","high"]),list=(value:string|null|undefined)=>value?.split(/\r?\n|,/).map(item=>item.trim()).filter(Boolean)??[];
async function adminContext(clientId:string){const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)throw new Error("Sign in as a partner.");const{data:membership}=await supabase.from("organization_members").select("organization_id").eq("user_id",user.id).eq("role","admin").eq("status","active").limit(1).maybeSingle();if(!membership)throw new Error("An active partner membership is required.");const{data:client}=await supabase.from("clients").select("id").eq("id",clientId).eq("organization_id",membership.organization_id).is("deleted_at",null).maybeSingle();if(!client)throw new Error("Client not found.");return{supabase,clientId:String(client.id)}}
export async function createBlog(formData:FormData):Promise<Result>{try{const clientId=String(formData.get("clientId")??""),title=String(formData.get("title")??"").trim(),keyword=String(formData.get("keyword")??"").trim();if(!title||!keyword)return{ok:false,error:"Title and target keyword are required."};const{supabase}=await adminContext(clientId);const{data,error}=await supabase.from("content_items").insert({client_id:clientId,content_kind:"blog",topic:title,target_keyword:keyword,concept:String(formData.get("brief")??"").trim(),seo_metadata:{intent:String(formData.get("intent")??"Commercial")},status:"research"}).select("id").single();if(error)return{ok:false,error:error.message};revalidatePath("/admin/blogs");return{ok:true,id:String(data.id)}}catch(error){return{ok:false,error:error instanceof Error?error.message:"Unable to create the article."}}}
export async function saveBlog(item:AdminBlog):Promise<Result>{const status=item.status.toLowerCase().replaceAll(" ","_");if(!blogStatuses.has(status))return{ok:false,error:"Invalid blog status."};try{const{supabase}=await adminContext(item.clientId);const{error}=await supabase.from("content_items").update({topic:item.title,target_keyword:item.keyword,concept:item.brief,body:item.excerpt,seo_metadata:{intent:item.intent,title:item.metaTitle,description:item.metaDescription},status,updated_at:new Date().toISOString()}).eq("id",item.id).eq("client_id",item.clientId).eq("content_kind","blog");if(error)return{ok:false,error:error.message};revalidatePath("/admin/blogs");return{ok:true}}catch(error){return{ok:false,error:error instanceof Error?error.message:"Unable to save the article."}}}
export async function generateBlogDraft(item:Pick<AdminBlog,"id"|"clientId"|"title"|"keyword"|"intent"|"brief">):Promise<GeneratedBlogResult>{if(!item.title.trim()||!item.keyword.trim())return{ok:false,error:"Add a title and target keyword before generating."};try{let context:GenerationContext,provider;if(isDemoMode()){provider=new DemoAIProvider();context={clientId:item.clientId,businessKnowledge:"ABC Interiors is a Dubai interior design and renovation studio serving Dubai and Sharjah.",services:["Kitchen Renovation","Villa Renovation","Wardrobes","Interior Fit-out"],offers:[],prohibitedClaims:["Unverified prices","Guarantees","Certifications","Testimonials","Statistics"]}}else{const{supabase}=await adminContext(item.clientId);const[clientResult,profileResult,servicesResult,locationsResult,faqsResult]=await Promise.all([supabase.from("clients").select("name,industry,city,country").eq("id",item.clientId).single(),supabase.from("business_profiles").select("description,target_customers,value_proposition,tone_of_voice,business_hours,offers,important_claims,prohibited_claims").eq("client_id",item.clientId).maybeSingle(),supabase.from("business_services").select("name").eq("client_id",item.clientId).eq("status","active"),supabase.from("business_locations").select("name").eq("client_id",item.clientId).eq("status","active"),supabase.from("business_faqs").select("question,answer").eq("client_id",item.clientId).eq("verified",true)]);const sourceError=[clientResult.error,profileResult.error,servicesResult.error,locationsResult.error,faqsResult.error].find(Boolean);if(sourceError)throw sourceError;const profile=profileResult.data;context={clientId:item.clientId,businessKnowledge:JSON.stringify({client:clientResult.data,profile,locations:locationsResult.data,verifiedFaqs:faqsResult.data,article:{title:item.title,intent:item.intent}}),services:(servicesResult.data??[]).map(service=>service.name),offers:list(profile?.offers),prohibitedClaims:list(profile?.prohibited_claims)};provider=createAIProvider()}const briefResult=await provider.generateBlogBrief(context,`${item.keyword}. Working title: ${item.title}. Existing research: ${item.brief||"none"}`),draftResult=await provider.generateBlogDraft(context,briefResult.data);const metaTitle=item.title.slice(0,60),plain=draftResult.data.replace(/[#*_`]/g,"").replace(/\s+/g," ").trim(),metaDescription=plain.slice(0,155).replace(/\s+\S*$/,"").trim()+((plain.length>155)?"…":""),article={brief:briefResult.data,excerpt:draftResult.data,metaTitle,metaDescription,status:"Draft" as const};if(!isDemoMode()){const{supabase}=await adminContext(item.clientId);const{error}=await supabase.from("content_items").update({concept:article.brief,body:article.excerpt,seo_metadata:{intent:item.intent,title:article.metaTitle,description:article.metaDescription},status:"draft",updated_at:new Date().toISOString()}).eq("id",item.id).eq("client_id",item.clientId).eq("content_kind","blog");if(error)throw error}revalidatePath("/admin/blogs");return{ok:true,article,provider:draftResult.provider,model:draftResult.model}}catch(error){return{ok:false,error:error instanceof Error?error.message:"Unable to generate the article draft."}}}
export async function createKeyword(formData:FormData):Promise<Result>{try{const clientId=String(formData.get("clientId")??""),priority=String(formData.get("priority")??"medium").toLowerCase();if(!priorities.has(priority))return{ok:false,error:"Invalid priority."};const{supabase}=await adminContext(clientId);const{data,error}=await supabase.from("seo_keywords").insert({client_id:clientId,keyword:String(formData.get("keyword")??"").trim(),intent:String(formData.get("intent")??""),target_url:String(formData.get("url")??"").trim(),current_position:Number(formData.get("current"))||null,previous_position:Number(formData.get("current"))||null,priority,status:"tracking",notes:String(formData.get("notes")??"").trim()||null}).select("id").single();if(error)return{ok:false,error:error.message};revalidatePath("/admin/seo");return{ok:true,id:String(data.id)}}catch(error){return{ok:false,error:error instanceof Error?error.message:"Unable to add the keyword."}}}
export async function updateOpportunity(id:string,clientId:string,status:string):Promise<Result>{const normalized=status.toLowerCase().replaceAll(" ","_");if(!opportunityStatuses.has(normalized))return{ok:false,error:"Invalid SEO status."};try{const{supabase}=await adminContext(clientId);const{error}=await supabase.from("seo_tasks").update({status:normalized,updated_at:new Date().toISOString()}).eq("id",id).eq("client_id",clientId);if(error)return{ok:false,error:error.message};revalidatePath("/admin/seo");return{ok:true}}catch(error){return{ok:false,error:error instanceof Error?error.message:"Unable to update the opportunity."}}}
