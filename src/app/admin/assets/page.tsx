import {AppShell} from "@/components/app-shell";import {AssetBrowser} from "@/components/asset-browser";import {loadAdminAssets} from "@/lib/admin-asset-data";
export default async function Assets({searchParams}:{searchParams:Promise<{client?:string}>}){const params=await searchParams,data=await loadAdminAssets(params.client);return <AppShell role="admin" title="Asset library" subtitle="Client-owned brand, original and generated files."><AssetBrowser initial={data}/></AppShell>}
