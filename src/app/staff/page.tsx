import {AppShell} from "@/components/app-shell";import {PostingQueue} from "@/components/posting-queue";import {loadStaffQueue} from "@/lib/staff-data";
export default async function Staff(){const queue=await loadStaffQueue();return <AppShell role="staff" title="Posting queue" subtitle="Everything internally reviewed and ready for manual publishing." actions={<span className="demo-badge">{queue.periodLabel}</span>}><PostingQueue initial={queue.items} clients={queue.clients} isDemo={queue.isDemo}/></AppShell>}
