"use server";
import {revalidatePath} from "next/cache";import {createClient} from "@/lib/supabase/server";import {assetCategories,categoryForFile,safeAssetName,type AssetCategory} from "@/lib/assets";
const allowed=new Set(["image/jpeg","image/png","image/webp","image/svg+xml","application/pdf","video/mp4"]),maxBytes=50*1024*1024;
export type UploadResult={ok:true;asset:{name:string;meta:string;label:string}}|{ok:false;error:string};
export async function uploadClientAsset(formData:FormData):Promise<UploadResult>{const file=formData.get("file"),requested=String(formData.get("category")??"");let clientId=String(formData.get("clientId")??"");if(!(file instanceof File)||!file.size)return {ok:false,error:"Choose a non-empty file."};if(file.size>maxBytes)return {ok:false,error:"Files must be 50 MB or smaller."};if(!allowed.has(file.type))return {ok:false,error:"This file type is not supported."};const category=(assetCategories as readonly string[]).includes(requested)?requested as AssetCategory:categoryForFile(file);let supabase;try{supabase=await createClient()}catch{return {ok:false,error:"Supabase Storage is not configured."}}const {data:{user}}=await supabase.auth.getUser();if(!user)return {ok:false,error:"Sign in before uploading."};if(!clientId){const {data}=await supabase.from("client_members").select("client_id").eq("user_id",user.id).limit(1).maybeSingle();clientId=data?.client_id??""}if(!/^[0-9a-f-]{36}$/i.test(clientId))return {ok:false,error:"A valid client is required."};const path=`${clientId}/${new Date().toISOString().slice(0,10)}/${crypto.randomUUID()}-${safeAssetName(file.name)}`;const uploaded=await supabase.storage.from("client-assets").upload(path,file,{contentType:file.type,upsert:false});if(uploaded.error)return {ok:false,error:uploaded.error.message};const inserted=await supabase.from("assets").insert({client_id:clientId,filename:file.name,mime_type:file.type,category,storage_path:path,source:"uploaded",created_by:user.id});if(inserted.error){await supabase.storage.from("client-assets").remove([path]);return {ok:false,error:inserted.error.message}}revalidatePath("/admin/assets");revalidatePath("/client/files");return {ok:true,asset:{name:file.name,meta:`${category} · ${Math.max(1,Math.round(file.size/1024))} KB`,label:file.type.startsWith("image/")?"Image":"File"}}}
