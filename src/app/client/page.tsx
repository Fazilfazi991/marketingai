import { AppShell } from "@/components/app-shell"; import { Panel, Status } from "@/components/ui"; import { access } from "@/lib/demo-data";
const delivery=[["Social posts","9 / 12"],["SEO articles","1 / 2"],["SEO review","Complete"],["Website check","Complete"],["Monthly report","Pending"]];
export default function Client(){return <AppShell role="client" title="Overview" subtitle="A clear view of your work, progress and next steps."><div className="overview-banner"><div><p>This month</p><h2>ABC Interiors</h2><p>Interior design & renovation · Dubai, UAE</p></div><span className="plan-chip">Growth · AED 999/month</span></div><div className="client-grid"><Panel title="September deliverables" meta="What we promised and what’s delivered"><div className="delivery-cards">{delivery.map(([label,value])=><div className="delivery-card" key={label}><span>{label}</span><b>{value}</b></div>)}</div></Panel><Panel title="Your connections" meta="Last verified Sep 6"><div className="access-list">{access.map(([name,state])=><div className="access-row" key={name}><span>{name}</span><Status tone={state==="Pending"?"warn":""}>{state}</Status></div>)}</div></Panel></div></AppShell>}
