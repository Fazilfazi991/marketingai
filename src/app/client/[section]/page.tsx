import { AppShell } from "@/components/app-shell"; import { ClientPortalSection } from "@/components/client-portal-section";import {ClientResultsDashboard} from "@/components/client-results-dashboard";
import {loadClientResults} from "@/lib/client-results";
const labels:Record<string,string>={leads:"Leads",traffic:"Traffic & SEO",reports:"Reports"};
export default async function Section({params}:{params:Promise<{section:string}>}){const {section}=await params;if(section==="leads"||section==="traffic"){const data=await loadClientResults();return <AppShell role="client" title={labels[section]} subtitle={`${data.clientName} · verified growth results.`}><ClientResultsDashboard data={data} view={section}/></AppShell>}return <AppShell role="client" title="Reports" subtitle="Your growth results."><ClientPortalSection/></AppShell>}
