import { AppShell } from "@/components/app-shell"; import { ClientPortalSection } from "@/components/client-portal-section";import {ClientResultsDashboard} from "@/components/client-results-dashboard";
const labels:Record<string,string>={leads:"Leads",traffic:"Traffic & SEO",reports:"Reports"};
export default async function Section({params}:{params:Promise<{section:string}>}){const {section}=await params;if(section==="leads"||section==="traffic")return <AppShell role="client" title={labels[section]} subtitle="Your verified growth results."><ClientResultsDashboard view={section}/></AppShell>;return <AppShell role="client" title="Reports" subtitle="Your growth results."><ClientPortalSection/></AppShell>}
