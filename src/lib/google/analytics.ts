import "server-only";
export type GoogleAccess={accessToken:string;ga4PropertyId:string;searchConsoleSiteUrl:string};export type ImportRange={from:string;to:string};
async function googlePost<T>(url:string,accessToken:string,body:unknown):Promise<T>{const response=await fetch(url,{method:"POST",headers:{Authorization:`Bearer ${accessToken}`,"Content-Type":"application/json"},body:JSON.stringify(body),cache:"no-store"});if(!response.ok)throw new Error(`Google API request failed (${response.status})`);return response.json() as Promise<T>}
export async function fetchGa4Report(config:GoogleAccess,range:ImportRange){return googlePost(`https://analyticsdata.googleapis.com/v1beta/properties/${encodeURIComponent(config.ga4PropertyId)}:runReport`,config.accessToken,{dateRanges:[{startDate:range.from,endDate:range.to}],dimensions:[{name:"date"},{name:"pagePath"}],metrics:[{name:"activeUsers"},{name:"sessions"}]})}
export async function fetchSearchConsoleReport(config:GoogleAccess,range:ImportRange){return googlePost(`https://searchconsole.googleapis.com/webmasters/v3/sites/${encodeURIComponent(config.searchConsoleSiteUrl)}/searchAnalytics/query`,config.accessToken,{startDate:range.from,endDate:range.to,dimensions:["date","query","page"],rowLimit:25000})}
