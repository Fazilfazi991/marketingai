import { describe,expect,it,vi } from "vitest"; import { getAutomationSnapshot,retryRun,runAutomation,setJobStatus } from "./automation-store";
describe("automation operations",()=>{it("pauses a reusable job",()=>{setJobStatus(1,"Paused");expect(getAutomationSnapshot().jobs.find(j=>j.id===1)?.status).toBe("Paused")});it("records a run lifecycle",()=>{vi.useFakeTimers();const before=getAutomationSnapshot().runs.length;runAutomation(1);expect(getAutomationSnapshot().runs).toHaveLength(before+1);expect(getAutomationSnapshot().runs[0].status).toBe("Running");vi.advanceTimersByTime(700);expect(getAutomationSnapshot().runs[0].status).toBe("Succeeded");vi.useRealTimers()});it("retries a failed run through its generic job",()=>{const before=getAutomationSnapshot().runs.length;retryRun(101);expect(getAutomationSnapshot().runs).toHaveLength(before+1)})});
