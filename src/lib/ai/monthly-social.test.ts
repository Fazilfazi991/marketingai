import { describe,expect,it } from "vitest"; import { addGeneratedSocialPlan,getSocialSnapshot,regenerateSocialImage } from "../social-store"; import { DemoAIProvider } from "./demo-provider"; import { prepareMonthlySocial } from "./monthly-social";
describe("monthly social preparation",()=>{it("creates twelve unique review-ready concepts from verified context",async()=>{const result=await prepareMonthlySocial(new DemoAIProvider(),"2026-12",12);expect(result.concepts).toHaveLength(12);expect(new Set(result.concepts.map(x=>x.topic))).toHaveProperty("size",12);expect(result.imageMode).toBe("placeholder");expect(result.estimatedCost).toBe(0);for(const item of result.concepts){expect(item.caption).toContain("ABC Interiors");expect(item.creativeBrief).toContain("no text overlay")}addGeneratedSocialPlan("2026-12",result.concepts,result.provider,result.model);const december=getSocialSnapshot().filter(x=>x.month==="December 2026");expect(december).toHaveLength(12);expect(december.every(x=>x.status==="Needs review")).toBe(true);expect(december.every(x=>x.imageStatus==="Placeholder"&&x.storagePath===null)).toBe(true);const first=december[0];regenerateSocialImage(first.id);expect(getSocialSnapshot().find(x=>x.id===first.id)?.imageVersion).toBe(2)});it("rejects invalid month input",async()=>{await expect(prepareMonthlySocial(new DemoAIProvider(),"December",12)).rejects.toThrow("YYYY-MM")})});
