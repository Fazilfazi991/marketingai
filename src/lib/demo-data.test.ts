import { describe, expect, it } from "vitest"; import { access, clients, deliverables, posts } from "./demo-data";
describe("Growth1000 demo fixture",()=>{it("contains the promised ABC Interiors operating data",()=>{expect(clients[0].name).toBe("ABC Interiors");expect(access).toHaveLength(6);expect(posts).toHaveLength(3);expect(deliverables.find(d=>d.label==="Social posts")?.total).toBe(120)});it("keeps social publishing human-controlled",()=>{expect(posts.some(p=>p.status==="Ready to schedule")).toBe(true);expect(posts.every(p=>p.status!=="Auto published")).toBe(true)})});
